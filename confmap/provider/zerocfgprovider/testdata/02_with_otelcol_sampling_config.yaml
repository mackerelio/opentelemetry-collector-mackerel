receivers:
  otlp:
    protocols:
      grpc:
        endpoint: localhost:4317
      http:
        endpoint: localhost:4318
processors:
  resource_detection:
    detectors: [env, system]
    system:
      hostname_sources: [os]
  probabilistic_sampler:
    sampling_percentage: 12.33
exporters:
  mackerel_otlp:
service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [resource_detection]
      exporters: [mackerel_otlp]
    traces:
      receivers: [otlp]
      processors: [probabilistic_sampler, resource_detection]
      exporters: [mackerel_otlp]
    logs:
      receivers: [otlp]
      processors: [resourcedetection]
      exporters: [mackerel_otlp]
