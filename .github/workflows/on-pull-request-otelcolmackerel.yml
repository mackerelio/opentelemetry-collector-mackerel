name: CI for pull requests (distributions/otelcol-mackerel)

on:
  pull_request:
    paths:
      - go.mod
      - go.mod
      - distributions/otelcol-mackerel/**
      - exporter/mackerelotlpexporter/**
      - confmap/provider/zerocfgprovider/**

jobs:
  build:
    uses: ./.github/workflows/wf-test-otelcolmackerel.yml
